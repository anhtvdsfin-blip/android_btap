<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/editNumber"
        android:hint="Nhập số nguyên"
        android:inputType="number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <RadioGroup
        android:id="@+id/radioGroup"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp">

        <RadioButton android:id="@+id/rbOdd" android:text="Số lẻ"/>
        <RadioButton android:id="@+id/rbEven" android:text="Số chẵn"/>
        <RadioButton android:id="@+id/rbPrime" android:text="Số nguyên tố"/>
        <RadioButton android:id="@+id/rbPerfect" android:text="Số hoàn hảo"/>
        <RadioButton android:id="@+id/rbSquare" android:text="Số chính phương"/>
        <RadioButton android:id="@+id/rbFibo" android:text="Số Fibonacci"/>
    </RadioGroup>

    <TextView
        android:id="@+id/tvMessage"
        android:text="Không có số nào thỏa mãn"
        android:gravity="center"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/holo_red_dark"/>

    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>
